# Wellstreet Frontend Map — 2026-09-04

**HEAD:** `c4762506acfbe4d22c4200840f4937c6f845c409` (branch `main`, commit `feat: ascetic-degen restyle — identity #4 (paper/ink/serif/green)`).

**Method note:** analysis-only inventory, produced from a full read of `site/index.html`, every module in `site/js/`, `site/css/style.css`, the four `api/` functions + `api/lib/shared.js`, `skills/wellstreet-vaults/SKILL.md`, all seven `docs/public/*.md`, and every file in `site-tests/` (headers + structure) and `api-tests/`. Every claim below carries a `file:line` citation; nothing is speculative. Zero code edits, zero commits, zero deploys. Line numbers refer to the worktree at HEAD `c476250` (the only non-frontend worktree deltas are `src/VaultFactory.sol` modified and `src/HarvesterV4.sol` / `test/fork/HarvesterV4Fork.t.sol` / `broadcast/` untracked — see Pending items #9).

**Architecture in one paragraph:** a zero-dependency, no-build static site (`site/`, served by Vercel per `vercel.json:1-6` — outputDirectory `site`, buildCommand copies `docs/public` → `site/docs/public`) that reads the chain DIRECTLY from the browser over public JSON-RPC (serverless-clean rule "D8": zero calls to its own `/api/*` routes, enforced by tests). Scripts load in fixed order (`site/index.html:548-557`): config → abi → amount → rpc → geo → vault → apr → wallet → docs → main. Identity #4 = paper/ink/serif/green ascetic-degen restyle (cream `#EDE9DC` substrate, ink `#1C1A15`, accent `#00A86B` fill / `#0d6b4f` text).

---

## 1. FEATURE INVENTORY

| # | Feature | Surface (section/component) | Files (file:line) | State | Notes |
|---|---------|------------------------------|-------------------|-------|-------|
| 1 | Sticky header: serif "Wellstreet" masthead plate + mono tag | `.site-header`, `.brand` | index.html:47-60; style.css:95-138 | live | Masthead = bordered paper-2 plate, uppercase serif (style.css:111-120); tag "yield vaults · tokenized stocks" mono uppercase |
| 2 | Section nav + scrollspy | `.site-nav` (4 anchors incl. `.nav-cta`) | index.html:53-58; style.css:122-138; main.js:1157-1180 | live | Scrollspy: IntersectionObserver, deepest-section-wins, one `.active` max, band rootMargin `-40%/-55%` (main.js:1178) |
| 3 | Nav CTA "Run it yourself" | `a.nav-cta` → `#docs` | index.html:57; style.css:128-137 | live | Ink pill; deliberately targets docs, not the deposit widget (style.css:125-127 comment) |
| 4 | Jurisdiction disclosure banner (mirrors only) | `#ws-jurisdiction-banner` | index.html:40-45; geo.js:82-95,128-135; style.css:141-148 | live (dormant) | Unhidden only on `.eth*` hosts / `?mirror=1`; gate itself UNWIRED per decision D14 (main.js:52-57 comment; config.js:247-253 retains `blockedCountries` unused by the page) |
| 5 | Noscript honesty note | `.noscript-note` | index.html:27-34; style.css:343 | live | Tells the user the page renders live reads in-browser and points at `/docs/public/` |
| 6 | Hero motif (green grid + quarter-turn SVG) | `.hero-motif` | index.html:72-84; style.css:554-568 | live | Inline SVG, aria-hidden, zero files/requests, z-index 0 behind `.wrap` (z-index 1), clipped by `.hero` overflow:hidden |
| 7 | Hero headline two-tone + punch word | `h1` (`.punch` / `.quiet`) | index.html:105; style.css:170-179, 711-717 | live | `--accent-punch #006B45` (light-paper flip inverted the punch direction); `text-wrap: balance` (style.css:71) |
| 8 | Hero CTA pair | `.cta-row` (`.cta-solid` → #deposit, `.cta-outline` → #docs) | index.html:110-113; style.css:315-333 | live | Per-class hover states; full-width stack ≤640px (style.css:447-448) |
| 9 | Hero lede + "checkable" framing | `p.lede` | index.html:109,114-118; style.css:180 | live | 60ch measure, 20px display role |
| 10 | Hero floating live-data chips (6: SPY/TVL/APR live + 3 static) | `.hero-chips` | index.html:96-103; main.js:332-358; style.css:719-819 | live | Live chips filled from the same pipeline reads (zero new hosts); degrade to label-only (`.hero-chip-val:empty` display:none, style.css:761); aria-hidden decorative duplicates; float + enter keyframes, scoped reduced-motion pair (style.css:812-819) |
| 11 | Hero live ledger ("Live ledger" panel, 4 rows) | `#hero-ledger` | index.html:125-148; main.js:241-319; style.css:572-606 | live | Consumes vault-card pipeline snapshots (no new RPC calls); rows: SPY/WETH slot0, Pool TVL (+≈USD), Pool protocol cut, Vault state; per-row honest "unavailable (RPC)" |
| 12 | Ledger stamp (WOW-8) | `.ledger-v.ledger-stamp[data-stamp]` | main.js:261,276,292,591-598; index.html:131-134; style.css:1021-1038 | live | CSS `content: '✓ ' attr(data-stamp)` names the read that verified (slot0/balances); data-carrying only; 900ms fade; reduced-motion keeps it visible at opacity 0.9 (style.css:1298) |
| 13 | Mint ticket card ("MINT TICKET", BACKED row `#mint-backed`) | `.mint-card.ledger-card` | index.html:156-163; main.js:763-778; style.css:618-644 | live | BACKED row = half of the single backingCoverage seam (see #19); callout is ink-inverted box |
| 14 | Hero facts trust row (4 overlapping pills) | `.hero-facts` / `.hero-fact` | index.html:164-181; style.css:181-216 | live | Keys/fee-split/provenance/audit-status; overlap via negative margin; stacks ≤640px (style.css:448-450) |
| 15 | Chain identity badge | `#chain-badge` | index.html:182; main.js:785-796 | live | `eth_chainId` probe per refresh; flag ●/△, honest "RPC unreachable" on failure |
| 16 | Stats band / live tape (4 cells: TVL, SPY USD, APR projected, split) | `#stat-tape` `.stat-band` | index.html:204-230; main.js:377-479; style.css:848-977 | live | Count-up reveal via IO+rAF once (main.js:441-479); every `var()` carries a mandatory literal fallback (style.css:837-847 comment); 2×2 grid ≤720px (style.css:903-905) |
| 17 | Tape delta ticks + band settle (WOW-1) | `.stat-tick`, `.stat-band--settle` | main.js:409-414,490-527; style.css:946-977 | live | ▲/▼/– glyph only on the two LIVE cells (TVL, price); projection cell and split cell excluded forever (main.js:404-408 comment, TAPE_TICK_IDS main.js:491) |
| 18 | Money-flow diagram (WOW-2) | `#flow-diagram` `.flow-figure` | index.html:245-275; main.js:600-651; style.css:1040-1137 | live | 3 HTML nodes bound to published reads; aria-hidden SVG dash-flow paced by class bucket from pool net rate (fast/mid/slow ≥40/≥10/>0, main.js:604-609); 90/10 split bar is geometry; vault node schematic→live via `flow-node--live` (main.js:630-646); SVG hidden ≤720px (style.css:1134-1137) |
| 19 | backingCoverage seam (one fill point, two cells) | `#mint-backed` + `#inv-stat` | main.js:763-778; vault.js:83-110,244-257; index.html:161,422 | live | Single `fillBackingCoverage` writes the IDENTICAL string to both cells; PENDING branch never issues the eth_call (isDeployed gate vault.js:249); truncation-toward-zero formatting ("99.9%", never "100.0%", vault.js:99-110) |
| 20 | Vault cards (state-driven pending/deployed) | `#vault-grid` `.vault-card` | index.html:278-296; main.js:141-220,780-837; style.css:240-280 | live (deployed, empty) | Rows: Vault contract status, Underlying (live), Underlying price (Chainlink + age + 24/5 note), Fee pool (TVL/fee tier/addr), Pool protocol cut (live slot0 nibbles), APR row + methodology-input row; pending variant = dashed card + `.pending-tag` (style.css:273-280) |
| 21 | Launch-fact single-source writer | `#vaults-launch-fact` | main.js:15-20,1274-1280; index.html:288 | live | `LAUNCH_FACT` constants are the only quoted state literals; static span byte-equal to `proseDeployed` (wow.test.js pins both sides); state-driven off the isDeployed seam |
| 22 | APR derivation pipeline (live sample → labeled baseline) | `WS.apr` | apr.js:39-309; main.js:839-907; config.js:222-239 | live | Client-side chunked `eth_getLogs` of pool Swap events (1h window, ≥20 events, real block timestamps, incomplete retrieval excludes the window — apr.js:181-267); fallback = phase-0 measured baseline 40.310%/yr clearly labeled (apr.js:901-906); depositor figure always labeled "projected, methodology-linked" (apr.js:39) |
| 23 | APR methodology footnote | `#apr-footnote` | index.html:291-294; apr.js:41-55; main.js:1269-1271 | live | Full formula chain text injected verbatim from `WS.apr.METHODOLOGY_FOOTNOTE` |
| 24 | Deposit / Redeem widget (approve→deposit; redeem/withdraw) | `#deposit` panels | index.html:298-361; main.js:909-1125; wallet.js:134-164; amount.js:22-73 | live, gated | Buttons disabled until wallet connected AND vault deployed (main.js:943-952); exact string→BigInt parse at the money boundary (amount.js; no float); each flow: wallet confirm → receipt polling → honest confirmed/reverted/pending outcome + explorer link (main.js:1049-1069,1115-1125) |
| 25 | Wallet connect + eip-6963 multi-wallet picker | `#btn-connect`, `#wallet-picker` | main.js:990-1031; wallet.js:19-63,69-103; style.css:340-342 | live | Discovery dedupe requires uuid+rdns (wallet.js:31-41); connect/chain-switch/add-chain (4902/-32603, wallet.js:89-103); accounts/chain change handlers reset state (main.js:1329-1332) |
| 26 | Honest error mapping (wallet flows) | `widgetStatus` | wallet.js:221-262; main.js:913-918 | live | 4001/-32002/4100/WS_WRONG_CHAIN mapped to plain sentences; Error(string) revert decode (0x08c379a0); unknown errors never report fake success |
| 27 | Balance + allowance readout | `#wallet-balances` | main.js:968-988; wallet.js:167-183 | live | eth_call balanceOf/allowance through the site RPC client (not the wallet) |
| 28 | Deposit simulator (WOW-6) | `#apr-sim` | index.html:372-395; main.js:682-732; style.css:1139-1228 | live | Slider 500–50000 USD; the ONLY math is size÷live-pool-TVL (dilution INPUT share, main.js:690-697); projection region static, consumes the published string byte-for-byte (main.js:729-732); source-slice gate forbids APR references between the SIM markers (main.js:682-727, enforced by wow.test.js) |
| 29 | Invariants section ("One token is one share") | `#invariants` | index.html:406-426; style.css:645-672 | live | Mono definition list (BALANCE/NEVER REBASES/MINT-REDEEM ARB) + "Backing Invariant" card carrying `#inv-stat` + definition-level `backingCoverage()` disclosure |
| 30 | Docs section with 7 tabs (proof-nav) | `#docs`, `#doc-tabs`, `#doc-pane` | index.html:429-448; docs.js:318-385; main.js:1131-1153; style.css:345-390 | live | Tabs: Compliance posture, Contract guarantees, What is not guaranteed, Risk disclosure, Run it yourself, APR methodology, Tokenomics (config.js:261-272); runtime relative fetch `../docs/public/<file>.md` (IPFS-ready); honest "Not published yet" state with self-test line (docs.js:331-341) |
| 31 | Markdown renderer (escaped-first, scheme-checked) | `WS.docs` | docs.js:19-70,87-221 | live | Everything HTML-escaped BEFORE formatting; javascript:/data:/vbscript: stripped after full decode (docs.js:29-42); supports fenced code, tables (scroll wrapper), lists, blockquote, hr |
| 32 | Docs TOC + heading deep links + code copy | `.doc-toc`, `.code-copy` | docs.js:75-81,230-246,252-316,267-275; main.js:1136-1150 | live | Per-doc slug ids `doc-<docId>-<slug>`; `#doc-…` hash loads THAT doc and scrolls (main.js:1138-1150); clipboard-only copy with honest "unavailable" (docs.js:252-261) |
| 33 | Scroll reveal (R3 IMP-3 + WOW-5) | `.ws-reveal` / `.scroll-reveal` | main.js:1190-1222; style.css:925-930,1230-1257 | live | Armed ONLY by JS (no-JS never sees hidden state); covers section heads, vault cards, band, flow figure, sim, footnote + hero-ledger row cascade and flow-node stagger |
| 34 | Curve divider draw-on | `.curve-divider .asset-draw` | index.html:456-458; main.js:1231-1247; style.css:1354-1376 | live | clip-path left-to-right on scroll-in; armed only by IO; reduced-motion = static full stroke |
| 35 | Chain-pulse delta flashes + heartbeat (WOW-7) | `.ledger-row.delta-up/down`, `#vaults-updated.heartbeat` | main.js:540-572; style.css:979-1019 | live | One-beat row background flash on published-value change (first render never flashes, main.js:544-547); stamp pulses on success, dims on failed cycle (main.js:113); connecting chip breathes (`is-connecting`, main.js:1260-1262) |
| 36 | Launch-flip choreography (WOW-3) | `body.launch-flip` | main.js:656-680,922-929; style.css:1259-1276 | live (armed, unfired) | Fires ONLY on a real false→true isDeployed transition observed while the page is open, once per session (sessionStorage `ws-launch-flip`); staggered per-region flip-in; never simulated |
| 37 | Design-kit asset hands + certificate | `.asset-point/-press/-magnify/-certificate` | index.html:147,332,440; main.js:160-165,574-588; style.css:1303-1393 | live | Self-hosted `site/img/` keepers (5 files); magnify sweeps once per refresh cycle gated by `motionAllowed()` (main.js:581-588); press-hand dips on deposit hover/focus (style.css:1364-1366); point-hand hidden ≤640px (style.css:1391-1393) |
| 38 | Hatched section separators | `.hero`, `section.block` border-image | style.css:150-157,218-221 | live | 8px repeating-linear-gradient 45° hatch via border-image; one gradient declaration per boundary |
| 39 | Agent-first section | `#agents` | index.html:466-482; main.js:1341-1354 | live | "Built for humans. Operated by agents." Skill link ships relative (`skills/wellstreet-vaults/SKILL.md`) and upgrades to `<repoUrl>/skills/…` only when `cfg.branding.repoUrl` becomes https (currently `PENDING_IDENTITY`) |
| 40 | Footer (ledger-grid) | `.site-footer`, `.footer-grid` | index.html:485-511; style.css:406-421 | live | 1px hairline ledger table (grid paints `--line` under a 1px gap); Protocol / Source & license / Honesty line columns; mono fine-print row with year stamp (`#footer-year`, main.js:1254-1256) and trademark note (`#trademark-note`, main.js:1268-1269) |
| 41 | Geo block page template | `#ws-geo-block` `<template>` | index.html:514-546; geo.js:101-136; style.css:423-434 | retained, unwired | NOT served by the protocol (index.html:514-521 comment); renders only if an operator ever configures a gate; F19 disclosure injected verbatim (geo.js:55,122-124); D14 retention keeps it byte-frozen |
| 42 | Theme system (paper/ink/green tokens) | `:root` ×3 | style.css:13-41,467-539,711-717 | live | Light-paper flip 2026-09-04; footer rides `--ink-deep #E2DCCB` re-pinned into the paper family (style.css:538); `theme-color #EDE9DC` + `color-scheme light` metas (index.html:17-18, html:2); exactly 3 radii (0 / 999px pill / 12px cards, style.css:530-535); type scale collapsed to 8 roles (style.css:508-523) |
| 43 | Motion system (reduced-motion pairs) | global + scoped guards | style.css:460-465,689-694,810-819,1283-1301,1382-1386; main.js:529-538 | live | Global guard nullifies ALL animation/transition; every shipped animation has a scoped static pair; JS motion gate `motionAllowed()` consults matchMedia (no matchMedia = no motion) |
| 44 | Self-hosted OFL webfonts | `@font-face` ×2 | style.css:43-59; site/fonts/ (44,336B + 48,256B + OFL.txt) | live | EB Garamond variable (display serif) + Inter variable (text sans), relative `../fonts/` paths (IPFS-ready), font-display: swap |
| 45 | Favicon (inline data-URI masthead plate) | `<link rel="icon">` | index.html:21-24 | live | Square paper-2 fill, 2px line border, serif W; zero new files (R3 IMP-11) |
| 46 | og:image / twitter:image | head metas | index.html:7-16 | live, stale art | 1200×630 `site/og.png` (2026-09-01, pre-restyle aesthetic — rebuild pending, see Pending #1) |
| 47 | Live refresh loop + updated stamp | `REFRESH_MS`/`STAMP_MS` timers | main.js:76-121,90-96 | live | 60s data refresh (visibility-gated, paused during wallet flows, main.js:98-114); 5s age stamp; timers unref'd for node --test |
| 48 | JSON-RPC client (retry/failover) | `WS.rpc` | rpc.js:30-334; config.js:71-81 | live | Pure decide/classifyFailure/backoffDelay + injectable fetch; 3 attempts/endpoint exp backoff 250→4000ms, then failover; batch cap 16 with sequential fallback for batch-hostile endpoints (rpc.js:226-249); built for the duplicate-ACAO CORS quirk of the primary RPC (rpc.js:5-17) |
| 49 | ABI/keccak layer (selectors derived at runtime) | `WS.abi` | abi.js:20-316; vault.js:11-12 | live | Full keccak-256 implementation, selectors from canonical signatures — no hardcoded possibly-wrong selectors anywhere |
| 50 | Jurisdiction gate module (pure, unwired) | `WS.geo` | geo.js:57-145 | dormant | Pure gateDecision/detectMirrorMode/applyGate + verbatim F19 constant; kept per D13/D14 with geo.test.js as the only 403 verification surface (geo.js:7-11) |

---

## 2. ENDPOINT INVENTORY

The shipped page makes **zero calls to its own `/api/*`** (D8 serverless-clean, `config.js:274-283`; enforced by `site-tests/render.test.js:2-11` — every runtime fetch must be one of the two RPC endpoints or a relative docs path — and `site-tests/resource-gate.test.js:1-11` — every on-disk HTML/CSS resource URL checked against a pinned host allowlist). The four `/api/*` functions exist as optional caching/UX enhancements.

| Endpoint / Read | Consumer (file:line) | Purpose | Cache / fallback |
|---|---|---|---|
| `GET /api/health` | api/health.js:24-61 | Liveness of the RPC path: REAL `eth_chainId` with failover; 200 only when chainId==4663; reports latency + per-upstream attempts | No cache — re-measures every call (api/health.js:9) |
| `GET /api/prices` | api/prices.js:164-209 | Tokenized-stock quotes (SPY, NVDA) from Chainlink `latestRoundData` on 4663 (8 dec); full provenance block; slot0 never the sole displayed price | `cache-control: public, max-age=30` (api/prices.js:45); no in-memory cache; secondary source Blockscout REST `exchange_rate` with browser-like UA (api/lib/shared.js:42-45,397-426); market-hours-aware staleness flag (weekday >4h = stale, weekend = expected, api/lib/shared.js:364-383) |
| `POST /api/rpc` | api/rpc.js:83-113 | JSON-RPC passthrough proxy with endpoint failover; relays write methods too (documented trust note, api/rpc.js:16-19) | No cache — must not serve stale chain state; 256KB body cap at 3 layers (api/rpc.js:10-14,25); 502 with attempts[] when all upstreams fail |
| `GET /api/vaults` | api/vaults.js:151-202 | Factory `vaultList()` → per-vault view fan-out (name/symbol/decimals/asset/totalAssets/totalSupply/pricePerShare/paused) as JSON | In-memory TTL 600s + `cache-control: public, max-age=60, s-maxage=600` (api/vaults.js:40,47); env `WELLSTREET_FACTORY_ADDRESS` unset → 200 `configured:false` empty list (api/vaults.js:110-120); failed registry read serves last good cache marked `staleCache:true`; per-vault failures tolerated inline |
| Shared upstreams (all 4 functions) | api/lib/shared.js:27-40 | `rpc.mainnet.chain.robinhood.com` primary → `robinhoodchain.blockscout.com/api/eth-rpc` secondary; env `WELLSTREET_RPC_URLS` overrides | Failover on ≥500/429 only (api/lib/shared.js:199); 10s timeout; `WELLSTREET_FACTORY_ADDRESS` + `WELLSTREET_RPC_URLS` are the only env knobs, no secrets |
| Browser read: public RPC `rpc.mainnet.chain.robinhood.com` | config.js:72-75; rpc.js:108-334; main.js:1295-1302 | ALL on-chain data the page shows: eth_chainId, eth_call batch reads (underlying symbol/decimals/paused/totalSupply vault.js:119-142; pool slot0/fee/balances/tokens vault.js:178-223; vault views vault.js:278-305; backingCoverage vault.js:248-257), eth_getLogs APR sampling (apr.js:183-215), receipt polling (wallet.js:189-204) | 3 attempts × exp backoff per endpoint then failover to Blockscout eth-rpc; per-response intermittent duplicate-ACAO CORS rejection is the reason the retry exists (rpc.js:5-17) |
| Browser read: Blockscout eth-rpc (secondary) | config.js:74; rpc.js failover path | Failover target for every RPC call | 429-limited under load in phase-0 (config.js:68 comment); re-probe before relying on it |
| Browser read: Chainlink SPY/USD feed via eth_call | config.js:131-140 (2 proxies); vault.js:145-173 | SPY USD price with age + 24/5 staleness flag (stale informational only, vault.js:166-169) | First readable proxy wins; negative answer rejected (vault.js:72-74); on total failure → "unavailable (feed)" row |
| Browser read: docs markdown | docs.js:318-342 via docUrl (docs.js:277-281) | Relative `../docs/public/<file>.md` (resolves to `site/docs/public/` — populated by vercel.json buildCommand) | Honest "Not published yet" state on any failure (docs.js:331-341) |
| Browser navigation: Blockscout explorer tx links | config.js:49-50; main.js:1115-1125 | "view on explorer" anchor after every sent tx | `<a target=_blank rel=noopener>` |
| Add-chain params (wallet) | config.js:51-57; wallet.js:89-103 | `wallet_addEthereumChain` for 4663 (rpc url + explorer) | Triggered on 4902/-32603 rejection |

---

## 3. AGENT SURFACE

**`skills/wellstreet-vaults/SKILL.md` (212 lines) — the canonical machine surface**, linked from the site's agent-first section (index.html:477). Coverage in brief:
1. **STATUS read-first block (fail-closed)** — every vault address DEPLOY-GATED, scam-drainer rule ("never approve or call any address not pinned in this skill"), pre-broadcast revert/empty expectations (SKILL.md:10-32).
2. **What the vaults are** — ERC-4626 YieldShares + Harvester fee-harvest model, no-share-mint yield crediting, VaultFactory registry, 48h timelock under a 2-of-3 Safe + revocable pause-only EOA (SKILL.md:34-36).
3. **Contract surface tables** — per-contract function/source/notes for YieldShares, Harvester, WellstreetTimelock, VaultFactory with `src/*.sol` line citations, incl. the 24-decimal share convention (SKILL.md:38-87).
4. **14-read keyless battery** — `cast call` commands + expected output shapes (totalAssets, convertToAssets @1e24, previewRedeem, backingCoverage, depositsPaused, feeBps, unaccountedAssets, harvester positionId/protocolAccrued/config, Harvested + YieldHarvested + VaultCreated getLogs, timelock readyAt) (SKILL.md:89-127).
5. **Write flows + 6 fail-closed rules** — approve-only-the-vault, bounded minOut, quote-asset-first, never direct-transfer (donations), state-check-before-write, pre-broadcast share tokens are not this protocol (SKILL.md:129-155).
6. **Governance & risk section** — timelock detection-not-prevention, 2-of-3-Safe-single-operator caveat, pause model, fee bounds, LP principal IL/LVR, single-pool concentration, issuer risk (SKILL.md:157-167).
7. **Honest reporting rules** — the ratified APR formula with every leg pinned, source+window+formula mandatory on every figure, backward-looking only, never-print-from-thin-data (<20 Swap events excluded), scenario tables as upper bounds (SKILL.md:169-190).
8. **Self-check** — grep battery an agent runs against its own copy (status present, no foreign branding, no overclaim language, source funcs exist) (SKILL.md:192-209).

**Config seam the site and the skill share (js/config.js as "THE single source of truth", config.js:2):**
- **isDeployed seam** — `WS.vault.isDeployed(addr)` (vault.js:30-32): a real 40-hex address that is not the `PENDING_DEPLOY` literal. Cards, hero ledger, flow diagram, widget gating, launch-fact writer, and the backingCoverage call/no-call branch ALL read this one seam (main.js:145,212,304,311-315,630,770,924,929,1074,1280).
- **LAUNCH_FACT writer** — single-sourced state literals (`pendingShort/pending/deployed/prosePending/proseDeployed`, main.js:20) written by init() into `#vaults-launch-fact` (main.js:1274-1280) and reused by the ledger row (main.js:311-315) and flow node (main.js:640-645); `wow.test.js` pins the static HTML span byte-equal to `proseDeployed`.
- **backingCoverage seam** — contract view `YieldShares.backingCoverage()` (1e18 fixed point; source of truth `src/YieldShares.sol:152` per vault.js:88); frontend decode/format pure functions (vault.js:92-110), single fill point writing both `#mint-backed` and `#inv-stat` (main.js:763-778); the skill's read battery exposes the same view to agents (SKILL.md:107).

---

## 4. PENDING / OPEN ITEMS

1. **og.png user-art rebuild.** `site/og.png` is the 2026-09-01 1200×630 asset from the previous identity; the ratified restyle doc marks its rebuild (paper/ink/green + logo badge composition) as a user-gated follow-up (`docs/internal/DESIGN_REFERENCE_ASCETIC_DEGEN_2026-09-04.md:70`), alongside favicon/lockup wiring decisions.
2. **D-D Vercel team migration.** `.vercel/project.json` still links the `wellstreet` project into the shared VIBE team org (`team_kTPfBOJqTflq6r6Zr7hPglmQ`); the runbook (`docs/ops/vercel-team-migration.md`, marked "INTERNAL — never committed") gates migration BEFORE the $WELL launch (`docs/internal/DECISIONS_2026-08-30.md:129`). Per the mapping-task brief (2026-09-04): GitHub auto-deploy is currently NOT firing — deploys are manual until migration. Machine-hygiene rules in the runbook: scoped-token auth only, `vercel whoami` back-checks (runbook §Machine hygiene).
3. **$WELL launch checklist.** Tokenomics doc: "$WELL has not launched" (`docs/public/tokenomics.md:3`); decision log pins the sequence — seed the harvester LP FIRST (creates real depositor APR), D12 fee-routing policy (creator share → harvester LP seed → treasury vaults), launch is user-operated via the pons launchpad (`docs/internal/DECISIONS_2026-08-30.md:139-141`; compliance.md:11 names ponsfamily.com as the launch venue).
4. **`repoUrl` is `PENDING_IDENTITY`** (config.js:35) — the agent-first skill link stays a relative repo path until identity ops (main.js:1341-1354); footer "Source repository publishes with the new identity at launch (placeholder until then)" (index.html:497).
5. **Vault family #2.** Planned via `VaultFactory` (one vault per asset; SKILL.md:211 "further multi-asset vaults … are planned"). USDG is already pinned as a live 4663 token (SKILL.md:24). Note: **no RBLX reference exists anywhere in the repo files** (grep across site/, skills/, docs/public/, src/ returned zero hits) — the #2-asset candidate naming lives outside this repository; nothing in the frontend is wired for a second vault (config.js `vaults` array has exactly one entry, config.js:147-157; main.js hardcodes `cfg.vaults[0]` at main.js:920).
6. **SKILL.md STATUS section is stale vs config.** The skill says "As of 2026-09-04 NOTHING is deployed … every vault command reverts or returns empty" (SKILL.md:12-17) while config.js pins the deployed addresses from the 2026-09-03 F-01 broadcast ("DEPLOYED 2026-09-03", config.js:92-96, 143-157). The site renders the deployed state; the skill still instructs agents in the pre-broadcast posture. Observation only — reconciling it is an owning-session decision.
7. **docs/public/methodology.md formula drift.** The methodology doc carries the superseded 2026-08-30 TVL-share form — "pool net fee APR × (harvester LP value ÷ target vault TVL) × (1 − protocol fee)" (docs/public/methodology.md:11,27,37-39) — while the shipped code uses the ratified 2026-09-03 liquidity-share form `pool_net × (L_pos/L_pool) × (pool_TVL/vault_TVL) × 0.9` (apr.js:5-11,88-93; config.js:184-211). The doc's own convention is "the repository is authoritative" (methodology.md is corrected in the same change — run-it-yourself.md:3). Observation only.
8. **`/api/vaults` provisional selectors.** `vaultList`/`totalAssets`/`pricePerShare`/`asset`/`aggregator` selectors are marked PROVISIONAL pending re-pin against the deployed source (api/lib/shared.js:53-69; api/vaults.js:21-23 note). The frontend never consumes this endpoint, so the exposure is agent/integration-only.
9. **Worktree state (uncommitted).** `src/VaultFactory.sol` modified; `src/HarvesterV4.sol`, `test/fork/HarvesterV4Fork.t.sol`, `broadcast/` untracked (git status at mapping time). Contract-side, outside this frontend map's scope, but parallel sessions must not assume a clean tree.
10. **Geo surfaces byte-frozen by design.** `js/geo.js`, the block template (index.html:514-546), banner (index.html:40-45) and geo CSS (style.css:423-434) are retained-but-unwired per D14 (main.js:52-57; DECISIONS_2026-08-30.md:110) — dormant, not dead code to delete without a decision.
11. **Tracked-but-not-yet-exercised on-chain state:** the deployed vault is EMPTY — the certificate keeper rides live cards too because "the deployed vault is empty; the empty card is its state" (main.js:157-159 comment); deposits activate at LP seed (flow node sub-label index.html:256).

---

## 5. SITE & API TEST SURFACE (one line each)

- `site-tests/abi.test.js` — pins keccak-256 against canonical digests and derived selectors against known vectors so a wrong selector can never ship (site-tests/abi.test.js:2-4).
- `site-tests/amount.test.js` — exact decimal string↔BigInt money-boundary vectors, no float anywhere in the path (amount.test.js:2-4).
- `site-tests/apr.test.js` — every published APR number must be recomputable from known phase-0-anchored inputs (apr.test.js:2-4).
- `site-tests/docs.test.js` — markdown renderer smoke + security: escape-before-format, unsafe URL schemes stripped (docs.test.js:2-4).
- `site-tests/geo.test.js` — the CANONICAL 403 verification surface for the (unwired) gate; F19 disclosure verbatim (geo.test.js:2-4).
- `site-tests/icon-cdn-guard.test.js` — zero font-awesome/cdnjs references in shipped site bytes (icon-cdn-guard.test.js:4-6).
- `site-tests/render.test.js` — full-boot integration under a DOM stub + mock fetch: page renders honestly, APR derives end-to-end labeled, and every fetch URL is RPC-or-relative-docs (the executable D8 gate) (render.test.js:2-11).
- `site-tests/render-degrade.test.js` — stats band under TOTAL RPC failure: '' not 0, APR shows the labeled baseline fallback, split stays static (render-degrade.test.js:2-10).
- `site-tests/resource-gate.test.js` — parses shipped HTML/CSS off disk and checks every static resource URL against a pinned host allowlist (closes render.test's fetch-only blind spot) (resource-gate.test.js:2-11).
- `site-tests/rpc.test.js` — the CORS-find mitigation: pure decision logic + full client loop with injected fetch, no network (rpc.test.js:2-6).
- `site-tests/theme.test.js` — FLIP-CARRIER-CONTRACT light-paper battery: pinned palette entries, head metas (theme-color/color-scheme), WCAG ≥4.5:1 for exactly six text pairs (theme.test.js:2-13).
- `site-tests/vault-coverage.test.js` — backingCoverage pure pins incl. the .9995→"99.9%" truncation discriminator (vault-coverage.test.js:2-5).
- `site-tests/wallet.test.js` — eip-6963 dedupe, receipt polling, receipt→honest-outcome mapping (wallet.test.js:2-4).
- `site-tests/wow.test.js` — wow helpers as pure units + sim source-slice no-recompute gate + launch-flip never fires under PENDING_DEPLOY (wow.test.js:2-10).
- API side: `api-tests/health|prices|rpc|vaults.test.js` + `shared-unit.test.js` drive each endpoint with fetch mocks (headers read; mocks.js provides req/res/fetch harnesses).

---

## 6. IMPROVEMENT HOOKS

Improvement candidates (filled by parallel sessions):

1. 
